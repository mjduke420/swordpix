# Keep the build context small + reproducible. The .godot/ import cache is
# regenerated by `godot --headless --import` inside the image, so exclude it.
.git
.gitignore
.godot/
build/
dist/
*.log
.DS_Store
DOCKER.md
