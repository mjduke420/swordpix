# Godot 4 import cache + editor state (regenerated by `godot --import`).
.godot/

# Export / build artifacts.
build/
dist/

# OS / logs / temp.
.DS_Store
Thumbs.db
*.log
*.tmp

# Local dev tooling (not part of the game / deploy).
addon.py
opencode.json
OPENCODE_TASKS.md
